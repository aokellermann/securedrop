version: 2

jobs:
  build:
    docker:
      - image: alpine

    steps:
      - checkout
      - run:
          name: Install required tools
          command: apk add --no-cache bash python3 py3-tornado py3-pycryptodome py3-validate-email openssl
      - run:
          name: Unit Test
          command: ./scripts/test.sh
